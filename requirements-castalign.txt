# castalign env — used by alignment/ and castalign_testground.ipynb
# Preprocessing/ may also work in this env (TBD — see README)
#
# castalign: installed EDITABLE from the local clone at D:\lab_local\programs\castalign
# PINNED to commit 1360831 (pre-v0.3) on branch pre-v0.3-rollback.
# DO NOT `git pull` or `pip install --upgrade castalign` — v0.3 has a
# QApplication-quit bug in the new align_interactive Qt dialog that breaks
# Mode D in castalign_testground.ipynb. See:
#   ~/.claude/projects/-home-ai-agent-agent-work-projects-cell-type-code-cell2cell-alignment/memory/project_castalign_v0_3_qapplication_bug.md
#
# `pip install -r` on this file resolves the line below from PyPI, which is
# v0.3+, and every other line is unpinned — a fresh resolve jumps napari 0.7.0
# -> 0.9.1 and drops the cupy/CUDA stack this env carries. Use it as a roster,
# not an install command.
#
# MOVING THE VENV: repair it, do not rebuild it. Built on Python 3.12.10 at
# C:\Users\David\AppData\Local\Python\pythoncore-3.12-64, which is outside
# lab_local and did not move; the venv's python.exe finds its own pyvenv.cfg by
# relative path, so it still runs from a new drive. Only two things break:
# castalign's editable finder (absolute path to the old programs\castalign) and
# the Scripts\*.exe launchers (stale shebangs — use `python.exe -m pip`,
# `python.exe -m jupyter lab`). Verified 2026-09-09 on the C: -> D: move:
#   .castalign-venv\Scripts\python.exe -m pip install -e <clone> --no-deps
# --no-deps is what keeps pip from re-resolving napari, numpy and CUDA.
castalign
numpy
scipy
tifffile
h5py
Pillow
scikit-image
matplotlib
pandas
imageio
ipywidgets
# `graphviz` (Python) is a thin wrapper that shells out to native `dot.exe`.
# On a fresh machine you ALSO need the system Graphviz install, which is NOT
# tracked here:
#   Windows:
#     1. winget install --id Graphviz.Graphviz
#     2. Append C:\Program Files\Graphviz\bin to user PATH manually -- winget
#        14.1.5 does NOT register PATH (verified 2026-05-07):
#          $d='C:\Program Files\Graphviz\bin'
#          $p=[Environment]::GetEnvironmentVariable('Path','User')
#          [Environment]::SetEnvironmentVariable('Path',"$p;$d",'User')
#     3. Fully relaunch JupyterLab from a fresh terminal (kernel restart alone
#        is NOT sufficient -- server inherits stale PATH).
#   macOS:    brew install graphviz   (PATH usually already wired up)
# Without it, `g.visualise()` raises ExecutableNotFound: WindowsPath('dot').
# See memory/project_graphviz_native_dep.md.
graphviz
